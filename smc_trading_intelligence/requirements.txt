# Every line is free and open source -- see COST_AUDIT.md.

# --- required ---------------------------------------------------------------
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
pydantic>=2.6
python-dotenv>=1.0
pytest>=8.0

# MT5 is Windows-only; on Linux/macOS the CSV/Parquet path is used instead.
MetaTrader5>=5.0.45 ; sys_platform == "win32"

# --- charts (Phase 17) ------------------------------------------------------
# `main.py chart` needs this. Everything else runs without it.
plotly>=5.20

# --- optional, and genuinely optional ---------------------------------------
# scipy  : exact Beta quantiles. Without it the estimator uses its own
#          normal-approximation fallback; the numbers move slightly, nothing breaks.
# sklearn: isotonic calibration. Without it a pool-adjacent-violators
#          implementation is used instead.
# Both are free; neither is imported at module load.
scipy>=1.11
scikit-learn>=1.4

# --- NOT required by anything ------------------------------------------------
# anthropic  -- only for `analyze --narrate` with ENABLE_CLAUDE=true. Absent
# package, absent key or absent network all fall back to local narration.
# Install it yourself if you want it:  pip install anthropic
