# Phase 1 (data layer). Every line is free and open source -- see COST_AUDIT.md.
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
pydantic>=2.6
python-dotenv>=1.0
pytest>=8.0

# MT5 is Windows-only; on Linux/macOS the CSV/Parquet path is used instead.
MetaTrader5>=5.0.45 ; sys_platform == "win32"
